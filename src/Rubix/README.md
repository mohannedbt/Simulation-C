# `src/Rubix/` — Rubik's cube / 3D example

This folder contains a 3D rotating-cube example that uses `rlgl`/`raylib` 3D API calls.

- `cube.c` — rotating cube demo (example of raylib 3D usage).

Build: compile with raylib linked; this example requires 3D support from the linked raylib build.
