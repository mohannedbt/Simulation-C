# `include/` — Public headers

This directory stores public header files used by demos. For example:

- `raygui.h` — header for the integrated `raygui` UI helper used by several demos.

When compiling, add `-Iinclude` to your compiler command so sources can find these headers.
