# `src/Osci/` — Oscillator demos

Contains simple spring/oscillator demos using raylib for rendering. Files:

- `oscilator.c` — traveling wave spring demonstration (main animation loop).
- `Oscilator.c` — (may be empty or alternate casing; keep in source tree for compatibility).

Build: compile the C file with raylib linked (see root `README.md`).
